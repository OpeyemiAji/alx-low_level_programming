#include <stdio.h>

/**
 * main - Prints if number is positive, zero or negative
 *
 * Return: Always (Success)
 */

int main(void)
{
int n;
char i;
for (i = 'a'; i <= 'z'; i++)
{
putchar(i);
}
putchar('\n');

return (0);
}
